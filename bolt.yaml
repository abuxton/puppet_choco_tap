modulepath: "./:./modules/:~/.puppetlabs/bolt-code/modules:~/.puppetlabs/bolt-code/site-modules"
inventoryfile: "~/.puppetlabs/bolt/inventory.yaml"
concurrency: 10
format: human
winrm:
  user: Administrator
  # password: 
  ssl-verify: false
  # tmpdir:
  ssl: true
  # realm:
  port: 5986
  # port: 5985 is ssl: false
   